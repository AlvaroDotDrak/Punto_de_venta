# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Python
.venv/
__pycache__/
*.pyc
*.pyo
pasteleria.db
# Archivos transitorios de SQLite (WAL) y respaldos locales de la DB
pasteleria.db-shm
pasteleria.db-wal
pasteleria.db.*

# Empaquetado PyInstaller (.exe)
release/
build_pyi/
*.spec

# Helper de instalación con el PIN dev real (nunca commitear; solo el .example)
crear_cuenta_soporte.bat

# Tooling de agentes (local, no es código del proyecto)
.claude/
.agents/

# Secrets — nunca commitear el .env real
.env
.env.local
.env.*.local

# Credenciales reales de la casilla de correo de fábrica (repo público).
# Solo el .example va al repo.
backend/mail_secrets.py

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Facturas reales de proveedores usadas para probar el escaneo con IA.
# Son documentos de clientes (RUT, folios, montos): no van al repo.
facturas_prueba/

# Copias de trabajo de bases de datos de clientes (dev_cliente.sh).
# Contienen datos reales del negocio y sus PINs: nunca al repo.
_clientes/
